# 主席模式新适性快速查验

日期：2026-09-22。结论：本轮检查通过，未发现新适性接入的业务阻断。未修改游戏逻辑或数值，未部署。

## 范围与结果

- 主席基础规则、世界、存储、编辑、繁育和赛事系列共75项针对性测试全部通过。日志：`artifacts/chairman-aptitude-quick-tests.txt`。
- 使用固定种子321创建日本80匹、美国80匹的参考世界，推进一个半月回合。生成15条赛事发生记录、45条出赛表现。
- 对真实推进路径产生的45次能力计算逐次核对：均使用 `track-aptitude-v1` 与有效赛程档案；覆盖草地、泥地和瞬发／持久／消耗三种类型；草泥修正、对应类型及强度修正与集中规则表一致；旧单马场修正 `courseMod` 为0。
- 160匹马均通过新适性字段验证。IndexedDB模拟环境中提交、读取后，新草泥与类型字段逐项一致；完整导出快照校验通过。
- 固定能力60、无气性／距离／重场影响，使用东京草2000瞬发Ⅱ：G与△得到 `60−20−4＝36`；A与◎得到 `60＋4＝64`。主席模式沿用不设60能力下限的既定规则。

小规模推进与固定条件结果：`artifacts/chairman-aptitude-smoke.json`。

## 解释与边界

后台马场配置仍存在 `courseType` 及少量“适性映射”的旧命名，但本轮实际能力计算未使用旧马匹地区适性或旧马场适性加减分；上述命名清理不属于本次快速验证。

本次为规则层、存储层与现有自动测试检查，没有重新执行主席页面的完整浏览器操作，也未运行长期世界模拟或多代繁育。BC/JBC轮换、编辑与繁育相关路径由上述既有测试覆盖。
